perf: offload discover_models to thread pool in FastAPI endpoints

Wrap synchronous blocking `discover_models` calls with `asyncio.to_thread()` in the `/models` and `/chat/start` API endpoints. This prevents the synchronous disk I/O of `os.scandir` from blocking the main asyncio event loop, significantly improving concurrent request handling.
